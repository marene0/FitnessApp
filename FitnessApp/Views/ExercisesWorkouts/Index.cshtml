@model IEnumerable<FitnessApp.Model.ExercisesWorkout>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Count)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ExercisesWorkoutId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ExercisesWorkoutId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ExercisesWorkoutId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
